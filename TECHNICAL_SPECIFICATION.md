# Техническое задание: Telegram-бот "GoalBuddy21"

## 1. ОБЩАЯ ИНФОРМАЦИЯ

### 1.1 Название проекта
**GoalBuddy21** — Telegram-бот для трёхдневного интенсива в Школе 21

### 1.2 Назначение
Бот помогает участникам интенсива:
- Сформулировать и зафиксировать личную цель обучения
- Оценить свой прогресс по достижению цели
- Получить мотивацию и поддержку от команды Школы 21

Все данные сохраняются анонимно в Google Sheets для последующего анализа фасилитаторами.

### 1.3 Целевая аудитория
- Участники трёхдневного интенсива в Школе 21
- Возраст: 18+
- Базовые навыки работы с Telegram

### 1.4 Технологический стек
- **Язык программирования**: Python 3.13+
- **Фреймворк бота**: python-telegram-bot 22.5
- **База данных**: Google Sheets (через gspread 5.12.0)
- **Планировщик задач**: APScheduler 3.10.4
- **Аутентификация**: oauth2client 4.1.3
- **Деплой**: Railway (Docker)
- **Логирование**: встроенный logging Python

---

## 2. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 2.1 Пользовательские сценарии

#### 2.1.1 День 1: Постановка цели
**Триггер**: Пользователь отправляет команду `/start`

**Сценарий**:
1. Бот отправляет приветственное сообщение с запросом цели
2. Пользователь вводит текст своей цели обучения
3. Бот валидирует ввод:
   - Минимум 10 символов
   - Максимум 500 символов
4. При успешной валидации:
   - Цель сохраняется в Google Sheets (анонимно)
   - Пользователь получает подтверждение с текстом своей цели
   - Информация о том, что можно изменить цель через `/start`
5. При ошибке валидации:
   - Отправляется сообщение с описанием ошибки и примером корректной цели

**Состояния**:
- `IDLE` → `/start` → `AWAITING_GOAL`
- `AWAITING_GOAL` → ввод цели → `GOAL_SET`

#### 2.1.2 День 3: Финальная оценка
**Триггер**: Пользователь отправляет команду `/assess`

**Сценарий**:
1. Бот проверяет, есть ли у пользователя установленная цель в текущей сессии
2. Если цели нет — отправляет сообщение с предложением поставить цель через `/start`
3. Если цель есть:
   - Отправляет запрос на оценку прогресса (0-100%)
   - Показывает пользователю его цель
4. Пользователь вводит число от 0 до 100
5. Бот валидирует ввод:
   - Должно быть целое число
   - Диапазон: 0-100
6. При успешной валидации:
   - Оценка сохраняется в Google Sheets
   - Отправляется благодарственное сообщение
7. При ошибке — запрос повторить ввод с корректным форматом

**Состояния**:
- `GOAL_SET` → `/assess` → `AWAITING_ASSESSMENT`
- `AWAITING_ASSESSMENT` → ввод оценки → `COMPLETED`

#### 2.1.3 Изменение цели
**Триггер**: Пользователь отправляет `/start` повторно

**Сценарий**:
1. Бот сбрасывает текущее состояние пользователя
2. Запускает сценарий постановки новой цели (2.1.1)
3. Создается новая анонимная запись в Google Sheets

### 2.2 Команды бота

| Команда | Описание | Доступность |
|---------|----------|-------------|
| `/start` | Начать работу и поставить цель обучения | Всегда |
| `/assess` | Оценить свой прогресс (0-100%) | После установки цели |

### 2.3 Текстовые сообщения

#### Приветственное сообщение (WELCOME_MESSAGE)
```
Привет! 👋
Я помогу тебе зафиксировать цель обучения на интенсиве в Школе 21.

Напиши, чего ты хочешь достичь во время обучения на интенсиве в Школе 21? Что тебе важно узнать, чему научиться для работы?
```

#### Подтверждение цели (GOAL_CONFIRMATION)
```
Отлично! 🎯
Твоя цель:
**"{goal}"**

Помни: ты сам заботишься о своей цели, а **команда Школы 21** рядом, чтобы поддерживать 💪
Если ты захочешь изменить цель, напиши мне /start
```

#### Запрос оценки (ASSESSMENT_REQUEST)
```
Оцени, пожалуйста, насколько процентов ты продвинулся к своей цели
**"{goal}"**

Введи число от 0 до 100 (0 — совсем не достиг, 100 — полностью достиг).
```

#### Благодарность за оценку (ASSESSMENT_THANKS)
```
Спасибо! 🌟
Твой процент достижения цели — **{percent}%**

Главное — не цифра, а осознание своего пути 💡

Спасибо, что получил этот опыт в Школе 21 🙌
```

#### Сообщения об ошибках
- **Неверная оценка**: "❌ Пожалуйста, введи число от 0 до 100. Например: 75"
- **Нет цели**: "❌ Сначала поставь цель с помощью команды /start"
- **Цель слишком короткая**: "❌ Пожалуйста, напиши цель более подробно (минимум 10 символов). Подумай, чем обучение может помочь твоей рабочей деятельности. Например: 'Освоить методы оптимизации и тестирования промптов, чтобы сокращать время на поисковые задачи и улучшать качество решений, предлагаемых AI.'"
- **Цель слишком длинная**: "❌ Цель слишком длинная. Пожалуйста, уложись в 500 символов."
- **Общая ошибка**: "❌ Произошла ошибка. Попробуй позже или обратись к организаторам."

### 2.4 Машина состояний (FSM)

```
┌─────────────────────────────────────────────────┐
│                    UserState                     │
├─────────────────────────────────────────────────┤
│ IDLE                  - Начальное состояние     │
│ AWAITING_GOAL        - Ожидание ввода цели      │
│ GOAL_SET             - Цель установлена          │
│ AWAITING_ASSESSMENT  - Ожидание оценки          │
│ COMPLETED            - Оценка завершена          │
└─────────────────────────────────────────────────┘

Переходы:
IDLE --[/start]--> AWAITING_GOAL
AWAITING_GOAL --[текст цели]--> GOAL_SET
GOAL_SET --[/assess]--> AWAITING_ASSESSMENT
AWAITING_ASSESSMENT --[число 0-100]--> COMPLETED
GOAL_SET --[/start]--> AWAITING_GOAL (сброс)
```

---

## 3. ХРАНЕНИЕ ДАННЫХ

### 3.1 Google Sheets

#### 3.1.1 Лист "UserData"

**Структура таблицы**:

| Столбец A | Столбец B | Столбец C | Столбец D |
|-----------|-----------|-----------|-----------|
| goal_text | goal_date | final_percent | final_date |

**Описание столбцов**:
1. **goal_text** (A) - Текст цели пользователя (строка, 10-500 символов)
2. **goal_date** (B) - Дата и время постановки цели (формат: `YYYY-MM-DD HH:MM:SS`)
3. **final_percent** (C) - Финальная оценка в процентах (целое число, 0-100)
4. **final_date** (D) - Дата и время оценки (формат: `YYYY-MM-DD HH:MM:SS`)

**Особенности**:
- Первая строка — заголовки столбцов
- Данные полностью анонимны (Telegram ID, username, имена НЕ сохраняются)
- Каждая новая цель = новая строка (даже от одного пользователя)
- Пустые значения в final_percent/final_date означают, что оценка еще не проведена

#### 3.1.2 Лист "Analytics"

**Назначение**: Место для будущих формул и дашбордов для фасилитаторов

**Текущее состояние**: Содержит только заголовок "Статистика по интенсиву"

**Возможные метрики** (для будущей реализации):
- Средний процент достижения целей
- Количество поставленных целей
- Количество завершенных оценок
- Распределение оценок по диапазонам

### 3.2 In-Memory хранилище состояний

**Структура**: Python словарь `user_states`

```python
user_states = {
    user_id: {
        'state': UserState,         # Текущее состояние
        'row_number': int,          # Номер строки в Google Sheets
        'goal_text': str            # Текст цели (для оценки)
    }
}
```

**Особенности**:
- Хранится только во время работы бота (не персистентное)
- При перезапуске бота все состояния сбрасываются
- Используется для связывания пользователя с его строкой в таблице
- Необходимо для команды `/assess` (чтобы знать цель пользователя)

---

## 4. АРХИТЕКТУРА ПРИЛОЖЕНИЯ

### 4.1 Структура проекта

```
goalbuddy21/
│
├── bot/                          # Модуль бота
│   ├── __init__.py
│   ├── main.py                   # Точка входа, инициализация
│   ├── handlers.py               # Обработчики команд и сообщений
│   ├── keyboards.py              # Клавиатуры Telegram
│   ├── messages.py               # Текстовые шаблоны
│   └── states.py                 # FSM состояния (Enum)
│
├── config/                       # Конфигурация
│   ├── __init__.py
│   └── settings.py               # Загрузка настроек из .env
│
├── database/                     # Работа с БД
│   ├── __init__.py
│   └── sheets.py                 # Google Sheets интеграция
│
├── scheduler/                    # Планировщик задач
│   ├── __init__.py
│   └── tasks.py                  # APScheduler (резерв для будущего)
│
├── utils/                        # Утилиты
│   ├── __init__.py
│   ├── logger.py                 # Настройка логирования
│   └── validators.py             # Валидация ввода
│
├── credentials/                  # Google API ключи
│   └── google_credentials.json   # Service Account JSON (НЕ в Git!)
│
├── logs/                         # Логи
│   └── bot.log                   # Файл логов (НЕ в Git!)
│
├── tests/                        # Тесты
│   └── __init__.py
│
├── .env                          # Переменные окружения (НЕ в Git!)
├── .env.example                  # Пример конфигурации
├── .gitignore                    # Исключения для Git
├── Dockerfile                    # Docker образ
├── requirements.txt              # Python зависимости
├── run.py                        # Скрипт запуска
├── start_bot.bat                 # Запуск для Windows
├── README.md                     # Документация
├── RAILWAY_DEPLOY.md             # Инструкция по деплою
└── TECHNICAL_SPECIFICATION.md    # Это ТЗ
```

### 4.2 Модули и их назначение

#### 4.2.1 bot/main.py
**Назначение**: Точка входа в приложение

**Основные функции**:
- `main()` — инициализация и запуск бота
  - Валидация конфигурации
  - Создание экземпляра Application (telegram-bot)
  - Инициализация соединения с Google Sheets
  - Регистрация обработчиков команд
  - Установка меню команд бота
  - Запуск polling (long polling)

**Обработчики**:
```python
CommandHandler("start", start_command)
CommandHandler("assess", assess_command)
MessageHandler(filters.TEXT & ~filters.COMMAND, handle_text_message)
application.add_error_handler(error_handler)
```

#### 4.2.2 bot/handlers.py
**Назначение**: Логика обработки команд и сообщений

**Функции**:
1. **start_command(update, context)**
   - Обработка команды `/start`
   - Устанавливает состояние `AWAITING_GOAL`
   - Отправляет приветственное сообщение

2. **assess_command(update, context)**
   - Обработка команды `/assess`
   - Проверяет наличие цели в текущей сессии
   - Устанавливает состояние `AWAITING_ASSESSMENT`
   - Отправляет запрос на оценку с целью пользователя

3. **handle_text_message(update, context)**
   - Обработка текстовых сообщений в зависимости от состояния
   - Состояние `AWAITING_GOAL`:
     - Валидация цели
     - Сохранение в Google Sheets
     - Переход в `GOAL_SET`
   - Состояние `AWAITING_ASSESSMENT`:
     - Валидация оценки (0-100)
     - Обновление строки в Google Sheets
     - Переход в `COMPLETED`

4. **error_handler(update, context)**
   - Глобальная обработка ошибок
   - Логирование
   - Отправка пользователю общего сообщения об ошибке

**Глобальная переменная**:
```python
user_states = {}  # {user_id: {'state': UserState, 'row_number': int, 'goal_text': str}}
```

#### 4.2.3 bot/messages.py
**Назначение**: Централизованное хранение текстов сообщений

**Константы**:
- `WELCOME_MESSAGE`
- `GOAL_CONFIRMATION`
- `ASSESSMENT_REQUEST`
- `ASSESSMENT_THANKS`
- `ERROR_INVALID_ASSESSMENT`
- `ERROR_NO_GOAL`
- `ERROR_GOAL_TOO_SHORT`
- `ERROR_GOAL_TOO_LONG`
- `ERROR_GENERAL`

**Требования**:
- Все тексты на русском языке
- Поддержка Markdown форматирования
- Плейсхолдеры для динамических данных: `{goal}`, `{percent}`

#### 4.2.4 bot/states.py
**Назначение**: Определение состояний FSM

**Классы**:
```python
class UserState(Enum):
    IDLE = "IDLE"
    AWAITING_GOAL = "AWAITING_GOAL"
    GOAL_SET = "GOAL_SET"
    AWAITING_PROGRESS = "AWAITING_PROGRESS"      # Резерв
    PROGRESS_RECORDED = "PROGRESS_RECORDED"      # Резерв
    AWAITING_ASSESSMENT = "AWAITING_ASSESSMENT"
    COMPLETED = "COMPLETED"

class ProgressOption(Enum):  # Резерв для Day 2
    ON_TRACK = "on_track"
    DIFFICULTIES = "difficulties"
    NOT_STARTED = "not_started"
```

#### 4.2.5 bot/keyboards.py
**Назначение**: Конфигурация клавиатур Telegram

**Функции**:
- `get_progress_keyboard()` — Inline клавиатура для Day 2 (резерв)
- `get_goal_options_keyboard()` — Клавиатура для изменения цели (резерв)
- `get_menu_button_config()` — Конфигурация persistent menu

**Текущее использование**: Минимальное (функционал в резерве)

#### 4.2.6 config/settings.py
**Назначение**: Загрузка и валидация конфигурации

**Класс Settings**:

**Атрибуты**:
```python
BOT_TOKEN: str                    # Telegram Bot Token
SPREADSHEET_ID: str               # Google Sheets ID
CREDENTIALS_PATH: str             # Путь к JSON с ключами
SCHEDULER_TIMEZONE: str           # Часовой пояс (Europe/Moscow)
LOG_LEVEL: str                    # Уровень логирования (INFO/DEBUG/WARNING)
LOG_FILE: str                     # Путь к файлу логов
TESTING_MODE: bool                # Режим тестирования
REMINDER_DELAY_SECONDS: int       # Задержка напоминаний (резерв)
```

**Методы**:
- `_setup_credentials_path()` — Умная загрузка credentials (из файла или env var)
- `validate()` — Проверка обязательных параметров
- `display()` — Вывод конфигурации (без чувствительных данных)

**Особенности**:
- Поддержка Railway: загрузка `GOOGLE_CREDENTIALS` из env var как JSON
- Автоматическое создание временного файла credentials в `/tmp/`
- Fallback на локальный файл при локальной разработке

#### 4.2.7 database/sheets.py
**Назначение**: Интеграция с Google Sheets API

**Класс SheetsDatabase**:

**Инициализация**:
```python
def __init__(self):
    # Авторизация через Service Account
    # Получение/создание листов UserData и Analytics
    # Инициализация заголовков таблиц
```

**Методы**:

1. **save_user_goal(goal_text: str) -> Optional[int]**
   - Сохраняет анонимную цель в новую строку
   - Заполняет: goal_text, goal_date
   - Возвращает номер строки

2. **get_goal_by_row(row_number: int) -> Optional[str]**
   - Получает текст цели по номеру строки
   - Используется для отладки (в продакшене не используется)

3. **save_final_assessment(row_number: int, percent: int) -> bool**
   - Обновляет столбцы C и D (final_percent, final_date)
   - Возвращает True при успехе

4. **_retry_on_rate_limit(func, *args, **kwargs)**
   - Retry механизм при превышении лимитов API
   - Экспоненциальный backoff (2, 4, 8 секунд)
   - До 3 попыток

**Singleton pattern**:
```python
_db_instance = None

def get_db() -> SheetsDatabase:
    global _db_instance
    if _db_instance is None:
        _db_instance = SheetsDatabase()
    return _db_instance
```

#### 4.2.8 scheduler/tasks.py
**Назначение**: Планировщик автоматических задач (резерв)

**Текущее состояние**: Модуль существует, но не используется в основном flow

**Функции** (для будущего расширения):
- `initialize_scheduler()` — Создание BackgroundScheduler
- `schedule_day2_reminder()` — Планирование напоминания через 24 часа
- `send_day2_reminder()` — Отправка напоминания пользователю
- `shutdown_scheduler()` — Остановка планировщика

**Примечание**: При добавлении функционала Day 2 необходимо интегрировать в `handlers.py`

#### 4.2.9 utils/logger.py
**Назначение**: Настройка логирования

**Функция setup_logger(name)**:
- Создает logger с двумя handlers:
  - FileHandler: запись в `logs/bot.log` (уровень DEBUG)
  - ConsoleHandler: вывод в консоль (уровень INFO)
- Формат: `[YYYY-MM-DD HH:MM:SS] [LEVEL] [name] message`
- Кодировка UTF-8 для поддержки русского языка

**Использование**:
```python
from utils.logger import logger

logger.info("Информационное сообщение")
logger.warning("Предупреждение")
logger.error("Ошибка")
logger.debug("Отладочная информация")
```

#### 4.2.10 utils/validators.py
**Назначение**: Валидация пользовательского ввода и функции безопасности

**Функции**:

1. **escape_for_sheets(s: str) -> str** ⚠️ БЕЗОПАСНОСТЬ
   - Экранирует текст для безопасной вставки в Google Sheets
   - **Защита от CSV/Formula injection**
   - Удаляет управляющие символы (ASCII < 0x20, кроме `\t`, `\n`)
   - Префиксирует строки, начинающиеся с `=`, `+`, `-`, `@` одинарной кавычкой `'`
   - Применяется ко ВСЕМ данным перед записью в Google Sheets

2. **safe_log_snippet(s: str, max_len=200) -> str** ⚠️ БЕЗОПАСНОСТЬ
   - Санитизирует пользовательский ввод для безопасного логирования
   - **Защита от log injection и утечки данных**
   - Удаляет `\n`, `\r` и другие управляющие символы
   - Ограничивает длину строки (защита от флуда логов)
   - Применяется ко всем пользовательским данным в логах

3. **validate_assessment_score(text: str) -> Tuple[bool, int]** ⚠️ БЕЗОПАСНОСТЬ
   - Строгая валидация оценки (0-100) с использованием regex
   - **Защита от injection через malformed input**
   - Паттерн: `^\s*(0|[1-9]\d?|100)\s*$`
   - Не использует `eval()` или небезопасные преобразования
   - Возвращает: (True, score) или (False, -1)

4. **validate_goal_text(text: str, min_length=10, max_length=500) -> Tuple[bool, str]**
   - Проверяет длину цели
   - Возвращает: (True, "") или (False, error_message)

---

## 5. НЕФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 5.1 Производительность

**Требования**:
- Время отклика бота: не более 2 секунд для 95% запросов
- Обработка сообщений: до 100 пользователей одновременно
- Retry механизм при ошибках Google Sheets API

**Ограничения Google Sheets API**:
- 300 запросов в минуту на проект
- 100 запросов в 100 секунд на пользователя
- Механизм exponential backoff обязателен

### 5.2 Надежность

**Требования**:
- Uptime: 99% (при деплое на Railway)
- Логирование всех ошибок
- Graceful handling ошибок API
- Fallback сообщения при недоступности Google Sheets

**Восстановление**:
- При падении бота состояния пользователей теряются (acceptable)
- Данные в Google Sheets сохраняются
- Пользователь может повторить `/start` для создания новой цели

### 5.3 Безопасность

> **📋 Полная документация**: См. [SECURITY.md](SECURITY.md) для детального описания всех мер безопасности

**Критические требования**:

#### 5.3.1 Защита от CSV/Formula Injection (ВЫСОКИЙ ПРИОРИТЕТ)

**Угроза**: Пользовательский ввод, начинающийся с `=`, `+`, `-`, `@`, может быть интерпретирован Google Sheets как формула.

**Последствия**:
- Выполнение формул (IMPORTXML, IMPORTDATA)
- Утечка данных через внешние запросы
- Выполнение макросов при экспорте в CSV/Excel

**Реализованная защита**:
- ✅ Функция `escape_for_sheets()` в `utils/validators.py`
- ✅ Автоматическое применение в `database/sheets.py::save_user_goal()`
- ✅ Удаление управляющих символов (ASCII < 0x20)
- ✅ Префикс `'` для строк, начинающихся с опасных символов

**Пример**:
```python
escape_for_sheets("=SUM(A1:A10)")  # → "'=SUM(A1:A10)"
```

#### 5.3.2 Анонимность пользователей (ВЫСОКИЙ ПРИОРИТЕТ)

**Требование**: Полная анонимность участников интенсива (п. 1.2, 5.3)

**Запрещено сохранять**:
- ❌ Telegram ID (`user.id`)
- ❌ Username (`@username`)
- ❌ Имя пользователя (`first_name`, `last_name`)
- ❌ Любые идентификаторы

**Разрешено**:
- ✅ Текст цели (анонимный)
- ✅ Даты (без привязки к пользователю)
- ✅ Оценка прогресса

**Реализация**:
- Google Sheets структура: 4 столбца БЕЗ идентификаторов
- Логирование БЕЗ `user_id`/`username`
- In-memory состояния (не персистентны)

#### 5.3.3 Безопасное логирование (ВЫСОКИЙ ПРИОРИТЕТ)

**Проблема**: Логирование `user_id` или необработанного ввода нарушает анонимность и создает риск log injection.

**Реализованная защита**:
- ✅ Функция `safe_log_snippet()` в `utils/validators.py`
- ✅ Все логи БЕЗ `user_id`, `username`
- ✅ Санитизация пользовательского ввода (удаление `\n`, `\r`, управляющих символов)
- ✅ Ограничение длины (200 символов)

**Правильное логирование**:
```python
# ❌ ПЛОХО
logger.info(f"User {user_id} saved goal: {goal_text}")

# ✅ ХОРОШО
logger.info(f"Saved goal to row {row_number}: {safe_log_snippet(goal_text)}")
```

#### 5.3.4 Строгая валидация ввода (ВЫСОКИЙ ПРИОРИТЕТ)

**Валидация оценки (0-100)**:
- ✅ Regex паттерн: `^\s*(0|[1-9]\d?|100)\s*$`
- ✅ Предотвращает injection: `"100; DROP TABLE"`, `"100\n"`, `"-5"`
- ✅ Не использует `eval()` или небезопасные преобразования

**Валидация цели**:
- ✅ Минимум: 10 символов (защита от мусора)
- ✅ Максимум: 500 символов (защита от флуда)

#### 5.3.5 Защита учетных данных

**Требования**:
- ✅ Токены и ключи ТОЛЬКО через переменные окружения
- ✅ `.env` и `credentials/` в `.gitignore`
- ✅ Service Account с минимальными правами
- ✅ Доступ только к одной конкретной таблице

**Запрещено**:
- ❌ Хардкод токенов в коде
- ❌ Коммит `.env` в Git
- ❌ Коммит `credentials/` в Git

#### 5.3.6 Обработка ошибок

**Требования**:
- ✅ Глобальный error handler (`bot/handlers.py::error_handler()`)
- ✅ Логирование ошибок БЕЗ раскрытия чувствительных данных
- ✅ Пользователю отправляется общее сообщение (не детали ошибки)
- ✅ Retry механизм для Google Sheets API (exponential backoff)

### 5.4 Масштабируемость

**Текущий масштаб**: 50-200 пользователей за 3-дневный интенсив

**Узкие места**:
1. Google Sheets API rate limits
2. In-memory хранилище состояний (не масштабируется горизонтально)

**Рекомендации для роста**:
- При >1000 пользователей: переход на реальную БД (PostgreSQL)
- Добавление Redis для персистентного хранения состояний
- Batch запросы к Google Sheets

### 5.5 Поддерживаемость

**Требования**:
- Модульная архитектура (разделение по компонентам)
- Документирование всех функций (docstrings)
- Централизация конфигурации
- Понятные имена переменных и функций
- Логирование ключевых событий

**Документация**:
- README.md — инструкция по запуску
- RAILWAY_DEPLOY.md — деплой на Railway
- TECHNICAL_SPECIFICATION.md — это ТЗ
- Inline комментарии в коде

### 5.6 Локализация

**Текущая версия**: Только русский язык

**Будущее расширение**:
- Вынос всех текстов в `messages.py`
- Возможность добавления файлов `messages_en.py`, `messages_kz.py`
- Определение языка по настройкам Telegram

---

## 6. КОНФИГУРАЦИЯ И ДЕПЛОЙ

### 6.1 Переменные окружения

**Файл .env** (для локальной разработки):

```env
# Telegram Bot
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz  # От @BotFather

# Google Sheets
SPREADSHEET_ID=1ABC...XYZ  # ID из URL таблицы
CREDENTIALS_PATH=credentials/google_credentials.json  # Путь к JSON

# Scheduler
SCHEDULER_TIMEZONE=Europe/Moscow

# Logging
LOG_LEVEL=INFO  # DEBUG | INFO | WARNING | ERROR
LOG_FILE=logs/bot.log

# Testing
TESTING_MODE=False  # True для тестирования (1 мин вместо 24 часов)
```

**Railway (production)**:

Вместо `CREDENTIALS_PATH` использовать:
```env
GOOGLE_CREDENTIALS={"type":"service_account","project_id":"..."}
```
(весь JSON в одну строку)

### 6.2 Google Cloud Setup

**Шаги**:

1. **Создать проект**:
   - Перейти: https://console.cloud.google.com/
   - New Project → "GoalBuddy21"

2. **Включить Google Sheets API**:
   - APIs & Services → Library
   - Найти "Google Sheets API" → Enable

3. **Создать Service Account**:
   - APIs & Services → Credentials
   - Create Credentials → Service Account
   - Имя: `goalbuddy21-bot`
   - Роль: None (минимальные права)

4. **Создать ключ**:
   - Открыть созданный Service Account
   - Keys → Add Key → Create New Key
   - Тип: JSON
   - Скачать → сохранить как `credentials/google_credentials.json`

5. **Создать Google Sheets таблицу**:
   - Открыть Google Sheets
   - Создать новую таблицу "GoalBuddy21 Data"
   - Скопировать SPREADSHEET_ID из URL:
     ```
     https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit
     ```

6. **Предоставить доступ**:
   - Открыть таблицу → Share
   - Вставить email Service Account (из JSON: `client_email`)
   - Права: Editor
   - Отправить

### 6.3 Локальный запуск

**Предварительные требования**:
- Python 3.13+
- Git

**Шаги**:

```bash
# 1. Клонировать репозиторий
git clone https://github.com/andreiparhomenco/bot21.git
cd bot21

# 2. Создать виртуальное окружение
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Создать .env файл
cp .env.example .env
# Заполнить реальными значениями

# 5. Добавить credentials
# Скопировать google_credentials.json в credentials/

# 6. Запустить бота
python run.py

# Или (Windows)
start_bot.bat
```

### 6.4 Docker

**Сборка образа**:
```bash
docker build -t goalbuddy21 .
```

**Запуск контейнера** (локально):
```bash
docker run --env-file .env \
  -v ./credentials:/app/credentials \
  -v ./logs:/app/logs \
  goalbuddy21
```

**Тестовый скрипт** (Windows PowerShell):
```powershell
.\docker-test.ps1
```

### 6.5 Railway Deployment

**Метод 1: GitHub Integration** (рекомендуется)

1. **Подготовка**:
   - Push проект в GitHub
   - Убедиться, что `.env` и `credentials/` в `.gitignore`

2. **Создать проект на Railway**:
   - Открыть: https://railway.app/new
   - Deploy from GitHub repo
   - Выбрать репозиторий
   - Railway автоматически обнаружит `Dockerfile`

3. **Настроить переменные**:
   - В Dashboard → Variables добавить:
     ```
     BOT_TOKEN=...
     SPREADSHEET_ID=...
     GOOGLE_CREDENTIALS={"type":"service_account",...}
     SCHEDULER_TIMEZONE=Europe/Moscow
     LOG_LEVEL=INFO
     TESTING_MODE=False
     ```

4. **Deploy**:
   - Railway автоматически соберет и запустит
   - При каждом push в main — автоматический редеплой

**Метод 2: Railway CLI**

```bash
# 1. Установить CLI
npm install -g @railway/cli

# 2. Логин
railway login

# 3. Инициализировать проект
railway init

# 4. Установить переменные
railway variables set BOT_TOKEN=...
railway variables set SPREADSHEET_ID=...
# ... остальные переменные

# 5. Deploy
railway up
```

**Мониторинг**:
- Логи: Dashboard → Deployments → Logs
- Метрики: CPU/Memory usage
- Статус: Health checks

---

## 7. ТЕСТИРОВАНИЕ

### 7.1 Unit тесты

**Фреймворк**: pytest

**Покрытие**:
- `utils/validators.py`:
  - `test_validate_assessment_score_valid()`
  - `test_validate_assessment_score_invalid()`
  - `test_validate_goal_text_too_short()`
  - `test_validate_goal_text_too_long()`
  - `test_validate_goal_text_valid()`

- `database/sheets.py`:
  - `test_save_user_goal()` (mock gspread)
  - `test_save_final_assessment()` (mock)

**Запуск**:
```bash
pytest tests/
```

### 7.2 Интеграционное тестирование

**Сценарий 1: Полный flow постановки цели**
1. Отправить `/start`
2. Ввести цель (валидную)
3. Проверить:
   - Получено подтверждение
   - Цель сохранена в Google Sheets
   - Состояние изменено на `GOAL_SET`

**Сценарий 2: Полный flow оценки**
1. Установить цель (Сценарий 1)
2. Отправить `/assess`
3. Ввести оценку (валидную)
4. Проверить:
   - Получена благодарность
   - Оценка сохранена в Google Sheets
   - Состояние изменено на `COMPLETED`

**Сценарий 3: Валидация ошибок**
1. Попытка `/assess` без цели → ошибка "Нет цели"
2. Ввод слишком короткой цели → ошибка "Слишком короткая"
3. Ввод оценки вне диапазона → ошибка "Введи число 0-100"

### 7.3 Ручное тестирование

**Чек-лист**:

- [ ] Команда `/start` отправляет приветствие
- [ ] Ввод цели (10-500 символов) сохраняется
- [ ] Цель отображается в Google Sheets
- [ ] Повторный `/start` создает новую запись
- [ ] Команда `/assess` запрашивает оценку
- [ ] Ввод оценки 0-100 сохраняется
- [ ] Оценка отображается в таблице в правильной строке
- [ ] Сообщения об ошибках отображаются корректно
- [ ] Русский язык отображается без искажений
- [ ] Emoji отображаются корректно
- [ ] Markdown форматирование работает (жирный текст)

### 7.4 Режим тестирования

**Активация**: `TESTING_MODE=True` в `.env`

**Изменения**:
- `REMINDER_DELAY_SECONDS` = 60 секунд (вместо 86400)
- Быстрое тестирование функционала Day 2 (если реализован)

---

## 8. ЛОГИРОВАНИЕ И МОНИТОРИНГ

### 8.1 Уровни логирования

| Уровень | Использование | Примеры |
|---------|---------------|---------|
| **DEBUG** | Детальная отладка | Содержимое переменных, детали запросов |
| **INFO** | Ключевые события | Запуск бота, сохранение данных, команды пользователей |
| **WARNING** | Предупреждения | Rate limit Google API, неожиданное поведение |
| **ERROR** | Ошибки | Сбой сохранения в БД, ошибки Telegram API |

### 8.2 Ключевые события для логирования

**Инициализация**:
```
[INFO] Starting GoalBuddy21 bot...
[INFO] ✅ Successfully connected to Google Sheets
[INFO] ✅ All handlers registered
[INFO] ✅ Bot is ready and polling for updates...
```

**Пользовательские действия**:
```
[INFO] User 123456789 (username) initiated /start
[INFO] ✅ Saved anonymous goal to row 42
[INFO] User 123456789 requested assessment
[INFO] ✅ Saved assessment for row 42: 85%
```

**Ошибки**:
```
[ERROR] ❌ Error saving user goal: [детали ошибки]
[ERROR] ❌ Update <update> caused error: [детали]
[WARNING] Rate limit hit, waiting 2s...
```

### 8.3 Файлы логов

**Локальная разработка**:
- Путь: `logs/bot.log`
- Ротация: вручную (при необходимости)
- Кодировка: UTF-8

**Railway**:
- Логи доступны в Dashboard → Logs
- Real-time streaming
- Хранение: последние 7 дней

---

## 9. ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ

### 9.1 Технические ограничения

1. **In-Memory состояния**:
   - При перезапуске бота все состояния теряются
   - Пользователи не могут продолжить прерванный flow
   - **Решение**: Пересоздать цель через `/start`

2. **Google Sheets как БД**:
   - Медленнее реальных СУБД
   - Rate limits (300 req/min)
   - Нет транзакций
   - **Приемлемо** для 50-200 пользователей

3. **Отсутствие персистентности пользователей**:
   - Нет связи между записями одного пользователя
   - Невозможно восстановить предыдущие цели
   - Полная анонимность = невозможность истории

4. **Планировщик не используется**:
   - Day 2 функционал (напоминания) не реализован
   - Модуль `scheduler/tasks.py` существует, но не активен

### 9.2 Функциональные ограничения

1. **Нет Day 2 напоминаний**:
   - Автоматические напоминания на второй день не работают
   - Резерв для будущей реализации

2. **Нет истории взаимодействий**:
   - Пользователь не может просмотреть свои предыдущие цели
   - Нет команды типа `/history`

3. **Нет редактирования цели**:
   - Можно только создать новую через `/start`
   - Нет inline кнопки "Изменить цель"

4. **Нет аналитики в реальном времени**:
   - Лист "Analytics" пустой
   - Фасилитаторы должны строить графики вручную

---

## 10. ROADMAP (БУДУЩИЕ УЛУЧШЕНИЯ)

### 10.1 Фаза 2: Day 2 Напоминания

**Описание**: Автоматическое напоминание через 24 часа после постановки цели

**Задачи**:
- [ ] Добавить сохранение Telegram ID (с согласием пользователя)
- [ ] Активировать `scheduler/tasks.py`
- [ ] Интегрировать `schedule_day2_reminder()` в `handlers.py`
- [ ] Добавить сообщение `REMINDER_MESSAGE` в `messages.py`
- [ ] Реализовать inline клавиатуру с выбором прогресса
- [ ] Сохранение ответа Day 2 в Google Sheets

**Оценка**: 8-12 часов разработки

### 10.2 Фаза 3: Персистентное хранилище

**Описание**: Переход на реляционную БД для хранения состояний

**Задачи**:
- [ ] Добавить PostgreSQL (через Railway)
- [ ] Создать таблицу `users` (user_id, username, state, goal_row)
- [ ] Миграция с in-memory на DB хранилище
- [ ] Восстановление состояний при перезапуске

**Оценка**: 16-20 часов

### 10.3 Фаза 4: Аналитика для фасилитаторов

**Описание**: Дашборд с метриками интенсива

**Задачи**:
- [ ] Формулы в листе "Analytics":
  - Средний процент достижения
  - Распределение оценок (0-25%, 26-50%, 51-75%, 76-100%)
  - Completion rate (сколько оценили из поставивших цели)
- [ ] Графики (через Google Sheets Charts)
- [ ] Экспорт в PDF

**Оценка**: 6-8 часов

### 10.4 Фаза 5: Многоязычность

**Описание**: Поддержка английского и казахского языков

**Задачи**:
- [ ] Создать `messages_en.py`, `messages_kz.py`
- [ ] Определение языка через `user.language_code`
- [ ] Динамическая загрузка текстов
- [ ] Команда `/language` для смены языка

**Оценка**: 10-12 часов

---

## 11. ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ (FAQ)

### 11.1 Для разработчиков

**Q: Почему Google Sheets, а не PostgreSQL?**
A: Простота интеграции и доступность для фасилитаторов без технических навыков. Для текущего масштаба (50-200 пользователей) достаточно.

**Q: Почему анонимность данных?**
A: Требование Школы 21 для защиты приватности участников интенсива. Нужны только цели и оценки, без персональных данных.

**Q: Как тестировать без развертывания?**
A: Локально с `TESTING_MODE=True` для быстрых итераций. Docker для проверки production окружения.

**Q: Можно ли масштабировать горизонтально?**
A: Нет, из-за in-memory `user_states`. Для масштабирования нужен переход на Redis или DB.

### 11.2 Для эксплуатации

**Q: Что делать при падении бота?**
A: Railway автоматически перезапустит. Данные в Google Sheets сохранены. Пользователи могут повторить `/start`.

**Q: Как добавить нового администратора к таблице?**
A: Google Sheets → Share → добавить email с правами "Viewer" (для просмотра) или "Editor" (для изменения).

**Q: Как изменить тексты сообщений?**
A: Отредактировать `bot/messages.py`, сделать commit, push на GitHub. Railway автоматически задеплоит.

**Q: Бот не отвечает. Как диагностировать?**
A:
1. Проверить логи: Railway Dashboard → Logs
2. Проверить переменные окружения
3. Проверить Google Sheets доступ (Service Account добавлен?)
4. Проверить BOT_TOKEN (не истёк?)

---

## 12. ГЛОССАРИЙ

| Термин | Описание |
|--------|----------|
| **Service Account** | Специальный тип Google аккаунта для серверных приложений (не требует OAuth) |
| **FSM** | Finite State Machine — конечный автомат для управления состояниями пользователя |
| **Long Polling** | Метод получения обновлений от Telegram (бот постоянно спрашивает сервер о новых сообщениях) |
| **Railway** | PaaS платформа для деплоя приложений (аналог Heroku) |
| **SPREADSHEET_ID** | Уникальный идентификатор Google таблицы (из URL) |
| **Inline Keyboard** | Кнопки, отображаемые под сообщением в Telegram |
| **Handler** | Функция-обработчик команд или сообщений в telegram-bot |
| **Exponential Backoff** | Алгоритм повторных попыток с увеличивающейся задержкой (1s, 2s, 4s, 8s...) |
| **Rate Limit** | Ограничение количества запросов к API в единицу времени |
| **Graceful Shutdown** | Корректное завершение работы с закрытием соединений и сохранением состояния |

---

## 13. КОНТАКТЫ И РЕСУРСЫ

### 13.1 Документация

**Проектная документация**:
- **Security Documentation**: [SECURITY.md](SECURITY.md) ⚠️ **ОБЯЗАТЕЛЬНО К ПРОЧТЕНИЮ**
- **Railway Deploy Guide**: [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md)
- **GitHub Push Guide**: [PUSH_TO_GITHUB.md](PUSH_TO_GITHUB.md)

**Внешние ресурсы**:
- **Telegram Bot API**: https://core.telegram.org/bots/api
- **python-telegram-bot**: https://docs.python-telegram-bot.org/
- **Google Sheets API**: https://developers.google.com/sheets/api
- **gspread**: https://docs.gspread.org/
- **APScheduler**: https://apscheduler.readthedocs.io/
- **Railway Docs**: https://docs.railway.app/

### 13.2 Репозитории

- **GitHub**: https://github.com/andreiparhomenco/bot21
- **Railway Project**: https://railway.app/dashboard (после создания)

### 13.3 Поддержка

Для вопросов по проекту:
- Создать Issue на GitHub
- Написать в Telegram: @jvityazeva-gif (или актуальный контакт)

---

## ПРИЛОЖЕНИЯ

### Приложение A: Примеры запросов к Google Sheets API

**Добавление строки**:
```python
sheet.append_row(["Освоить Python", "2025-10-31 14:30:00", "", ""])
```

**Обновление ячеек**:
```python
sheet.update('C2:D2', [[85, "2025-11-03 16:45:00"]])
```

**Чтение данных**:
```python
all_data = sheet.get_all_values()
# Возвращает список списков
```

### Приложение B: Структура user_states

```python
user_states = {
    123456789: {
        'state': UserState.GOAL_SET,
        'row_number': 42,
        'goal_text': 'Освоить методы оптимизации промптов'
    },
    987654321: {
        'state': UserState.AWAITING_ASSESSMENT,
        'row_number': 43,
        'goal_text': 'Научиться писать Unit-тесты'
    }
}
```

### Приложение C: Пример Dockerfile с комментариями

```dockerfile
# Базовый образ Python 3.13 (без лишних пакетов)
FROM python:3.13-slim

# Рабочая директория внутри контейнера
WORKDIR /app

# Переменные для оптимизации Python
ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1

# Установка системных зависимостей (gcc для компиляции пакетов)
RUN apt-get update && \
    apt-get install -y --no-install-recommends gcc && \
    rm -rf /var/lib/apt/lists/*

# Копирование и установка Python зависимостей
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Копирование кода приложения
COPY . .

# Создание директорий для логов и credentials
RUN mkdir -p logs credentials

# Указание порта (Railway требует, даже если не используется)
EXPOSE 8080

# Запуск приложения
CMD ["python", "run.py"]
```

### Приложение D: Пример .env.example

```env
# ========================================
# GoalBuddy21 Configuration
# ========================================

# Telegram Bot Token (получить от @BotFather)
BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# Google Sheets ID (из URL таблицы)
SPREADSHEET_ID=YOUR_SPREADSHEET_ID_HERE

# Google Credentials
# Локально: путь к JSON файлу
# Railway: весь JSON в одну строку в переменной GOOGLE_CREDENTIALS
CREDENTIALS_PATH=credentials/google_credentials.json

# Scheduler Settings
SCHEDULER_TIMEZONE=Europe/Moscow

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/bot.log

# Testing Mode (True = 1 min delays, False = 24h delays)
TESTING_MODE=False
```

---

**КОНЕЦ ТЕХНИЧЕСКОГО ЗАДАНИЯ**

---

## ИСТОРИЯ ИЗМЕНЕНИЙ

| Версия | Дата | Изменения |
|--------|------|-----------|
| 1.0 | 2025-10-31 | Первая версия технического задания |
| 1.1 | 2025-11-13 | **Security Hardening**: Добавлена защита от CSV/Formula injection, log injection, строгая валидация, анонимное логирование. Создан документ SECURITY.md |

---

**Текущая версия**: 1.1 (Security Hardened)  
**Дата последнего обновления**: 2025-11-13  
**Статус**: Финальная версия для реализации

